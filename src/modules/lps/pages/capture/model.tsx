export function useCaptureModel() {
  // Para uma landing page simples, não precisamos de muita lógica complexa
  // A captura será feita via formulário que envia direto para webhook

  return {
    // Placeholder para futuras funcionalidades se necessário
  };
}
